// Komponen 3D Background - DISABLED
// Semua file 3D tetap ada di folder assets, hanya tidak di-render
export default function Fish3DBackground() {
  // Return null untuk menonaktifkan semua 3D di background
  // File-file 3D tetap ada di folder assets dan tidak dihapus
  return null;
}
